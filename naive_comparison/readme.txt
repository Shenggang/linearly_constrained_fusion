The code here is used to compare LCF with two other naive sampler. The tests are contained in the R scripts in the \scripts folder.

convergence_test_***.R computes and compares the performance for different settings.